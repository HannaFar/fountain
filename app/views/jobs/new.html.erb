<% if @job.errors.full_messages.any? %>
  <% @job.errors.full_messages.each do |error_message| %>
    <div class="alert alert-danger"><%= error_message if @job.errors.full_messages.first == error_message %> </div>
  <% end %>
<% end %>
<h1>New Job</h1>
<%= render 'form' %>
<%= link_to 'Back', jobs_path %>
